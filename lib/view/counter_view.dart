import 'package:flutter/material.dart';

class counterView extends StatelessWidget {
  const counterView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: const Text("Hola mundo"));
  }
}
